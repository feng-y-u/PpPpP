---
name: opendesign
description: Start a design task — HTML pages, slide decks, interactive prototypes, UI kits, brand systems. Follows the OpenDesign workflow.
argument-hint: [design task description]
---

用户输入了 `/opendesign {{ARGUMENTS}}`。使用 `mcp__skillforge__skills__invoke` 工具调用名为 `opendesign` 的 skill，将用户的参数作为 input 传入，并按照 skill 返回的指令执行。
